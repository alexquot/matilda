{
  "name": "@albnnc/matilda",
  "version": "2.0.2",
  "description": "Material Design theme for Bootstrap 4",
  "main": "index.js",
  "scripts": {
    "lint": "stylelint 'scss/**/*.scss' --syntax scss --fix",
    "build": "gulp build",
    "start": "npm run build && gulp start",
    "list-custom": "cat scss/_variables.scss | grep Matilda | sed -e 's|\\s*//\\s*Matilda||'"
  },
  "author": "albnnc",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.1.2"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-sass": "^4.0.1",
    "node-sass-tilde-importer": "^1.0.2",
    "stylelint": "^8.4.0",
    "stylelint-config-idiomatic-order": "^5.0.0"
  }
}
